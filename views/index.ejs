<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Image to Text Converter</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
  <div class="max-w-4xl mx-auto mt-10 p-6 bg-white rounded-lg shadow-md">
    <h1 class="text-2xl font-bold mb-4">Image to Text Converter</h1>

    <form action="/convert" method="POST" enctype="multipart/form-data" class="space-y-4">
      <label class="block">
        <span class="text-gray-700">Upload an image</span>
        <input type="file" name="image" required class="block w-full mt-2 text-gray-700 bg-gray-100 rounded-lg">
      </label>

      <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
        Convert to Text
      </button>
    </form>

    <!-- Display the result -->
    <div class="mt-6">
      <% if (result) { %>
        <h2 class="text-xl font-semibold mb-2">Extracted Text:</h2>
        <pre class="bg-gray-100 p-4 rounded-lg"><%= result %></pre>
      <% } %>

      <% if (error) { %>
        <p class="text-red-500"><%= error %></p>
      <% } %>
    </div>
  </div>
</body>
</html>
